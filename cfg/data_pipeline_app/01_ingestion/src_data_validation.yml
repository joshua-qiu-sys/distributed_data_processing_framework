ingest~dataset1:
  req_validations:
    primary_key_validation:
      cols_to_check: [product_id]
    null_validation:
      cols_to_check: [item, quantity]

ingest~dataset2:
  req_validations:
    primary_key_validation:
      cols_to_check: [product_id]